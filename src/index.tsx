import React from 'react';
import ReactDOM from 'react-dom';
import BookmarkApp from './Bookmarks/BookmarkApp';

ReactDOM.render(
  <React.StrictMode>
    <BookmarkApp />
  </React.StrictMode>,
  document.getElementById('root')
);
